[package]
name = "enkrypton"
version = "0.0.0"
description = "🗨️Message anybody on the tor network\n🔒E2E - RSA\nThis app was created for my facharbeit"
authors = ["sshcrack"]
license = ""
repository = ""
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[build-dependencies]
tauri-build = { version = "1.4", features = [] }

[dependencies]
tauri = { version = "1.4", features = [
    "window-set-maximizable",
    "window-set-decorations",
    "window-maximize",
    "shell-open",
] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0.75"
actix-web = "4.3.1"
lazy_static = "1.4.0"
sha2 = "0.10.7"
hex = "0.4.3"
tauri-plugin-log = { git = "https://github.com/tauri-apps/plugins-workspace", branch = "v1", features = [
    "colored",
] }
log = "^0.4"
async-channel = "1.9.0"
signal-hook = "0.3.17"
sysinfo = "0.29.8"
rand = "0.8.5"
ts-rs = "7.0.0"
actix-web-actors = "4.2.0"
actix = "0.13.0"
tokio-tungstenite = "0.20.0"
tokio-socks = "0.5.1"
url = "2.4.0"
rustls = "0.21.7"
webpki-roots = "0.25.2"
async-rustls = "0.4.0"
smol = "1.3.0"
tokio = { version = "1.32.0", default-features = false, features = ["net"] }
futures-util = "0.3.28"

[dev-dependencies]

[features]
# this feature is used for production builds or when `devPath` points to the filesystem
# DO NOT REMOVE!!
custom-protocol = ["tauri/custom-protocol"]
